name: example

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: ./gradlew test
        with:
          message:
              Hello World!
